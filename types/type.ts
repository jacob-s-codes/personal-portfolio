export interface SpotlightProps {
    className?: string; // Optional class names for styling
    children: React.ReactNode; // React elements to render inside the border
    spotlightColor: string; // The color of the spotlight effect (e.g., "rgba(255, 0, 0, 0.5)")
  }
  